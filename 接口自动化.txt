根据接口文档编写用例-》拿到用例的数据去测试-》拿到测试结果写入excel

HttpClient:
来替代客户端角色发起http请求
Testng:
1.更好的管理咱们的用例
2.提供丰富的测试场景
3.通过它来设计测试流程
POI
读excel用例，接口数据，用例数据
写：测试结果，数据验证的结果，测试过程中用到变量值

fastJOSON
来解析JSon格式的字符串--》对象
对象--》转化为JSON格式的字符串
实现字符串跟对象之间的相互转换

mysql-connector-java
mysql的数据驱动，是希望通过JDBC技术实现数据库表的查询

log4j
记录测试框架重要的测试流程
将有用的数据记录在日志里面，方便我们跟踪问题


数据验证，处理接口的数据验证
1.需要执行几条SQL
2.传过去的SQL

接口鉴权
权限鉴定，在调用接口前判断用户是否已经登录完成
一般不会做权限控制的接口，注册。登录接口

对于接口鉴权的接口项目，处理接口调用的
1.先登录
2.在调用其他做了权限控制的接口

常见鉴权框架的鉴权方式。
1.jsessionID相当于是一张票，当你登录完，服务器就会返回这样的一张票给你（在响应头里面有一个Set-cookie,放的就是服务器返回票的信息）
后面在请求的时候会默认带着这张票过去，服务器会判断请求的头里面是否有Cookie头,并且Cookie头里的JessionID是否跟服务器的会话ID一致
如果一致，就说明已经登录过了。否则认为还未登录
2.服务器返回的Ticket或者TOken 那接口的调用要带上这个TICKET或者TOKEN,至于怎么带，也可以放在请求头里面，直接拼接在URL
上面，请找开发人员确认



在执行接口用例测试的时候，什么时候需要做数据验证
--当这调用例的执行有影响到表数据，那么就必须要做必要的数据验证。

接口前数据验证
接口调用
接口后数据验证

在读文件的时候，文件路径数据


1.编写用例数据，设计测试参数
2.更具接口业务，判断接口用例是否需要做数据验证
3.编写测试类，继承uniformProcessor，并实现父类的datas抽象方法，作为自己的数据提供者
3.在testng.xml中添加一个test集，并指定测试类的路径
